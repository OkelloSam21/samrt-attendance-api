The error indicates that the DNS lookup for `smartattendance.azurecr.io` failed during the login step to the Azure Container Registry. Here are some steps to resolve this issue:

1. **Check DNS Resolution**: Ensure that the runner can resolve the DNS for `smartattendance.azurecr.io`. This might involve checking network configurations or using a different DNS server.

2. **Verify Secrets**: Ensure that `AZURE_ACR_PASSWORD` and `AZURE_ACR_USERNAME` secrets are correctly set in your repository settings.

3. **Retry Login**: Sometimes, intermittent network issues can cause DNS resolution failures. Retry the workflow to see if the issue persists.

4. **Update Docker Login Command**: Ensure that the login command is correct. You might want to include a debug step to verify the output.

Here is an updated version of the workflow with an additional step to verify DNS resolution:

```yaml
name: Build & Deploy Ktor Backend

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify DNS Resolution
        run: nslookup smartattendance.azurecr.io

      - name: Log in to Azure Container Registry
        run: |
          echo "${{ secrets.AZURE_ACR_PASSWORD }}" | docker login smartattendance.azurecr.io -u ${{ secrets.AZURE_ACR_USERNAME }} --password-stdin

      - name: Build Docker image
        run: docker build -t smartattendance.azurecr.io/ktor-backend:latest .

      - name: Push Docker image to ACR
        run: docker push smartattendance.azurecr.io/ktor-backend:latest

      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v2
        with:
          app-name: "smartattendance-backend"
          images: "smartattendance.azurecr.io/ktor-backend:latest"
        env:
          AZURE_WEBAPP_PUBLISH_PROFILE: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
```

This will help verify if the DNS resolution is the issue and provide more context for troubleshooting.
